# eslint-plugin-react-hooks@4.6.2 (in client/) only declares a peer range up
# to ESLint 8, but works fine with the ESLint 9 flat config already in this
# project. legacy-peer-deps avoids the ERESOLVE conflict without touching
# the existing lint setup.
legacy-peer-deps=true
