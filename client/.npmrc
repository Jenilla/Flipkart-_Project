# See root .npmrc for why this is needed (eslint-plugin-react-hooks peer
# range vs. ESLint 9). Duplicated here so `npm install` still works if you
# cd into client/ directly instead of using the root install-all script.
legacy-peer-deps=true
